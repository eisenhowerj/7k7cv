[project]
name = "infra"
version = "0.1.0"
description = "CDK infrastructure for the 7K7 static site"
requires-python = ">=3.11"
dependencies = [
  "aws-cdk-lib>=2.130.0,<3.0.0",
  "constructs>=10.0.0,<11.0.0",
]

[dependency-groups]
dev = [
  "pytest>=7.0",
  "pytest-cov",
]
